{% extends 'partials/base.html.twig' %}

{% block body_class %}page-home{% endblock %}

{% block content %}
{# Hero Section #}
<section class="hero">
  <div class="hero__bg">
    <img src="{{ url('theme://images/hero/norrbotten.jfif') }}" alt="Skogslandskap i Norrbotten" loading="eager">
  </div>
  <div class="hero__overlay"></div>
  <div class="hero__content">
    <h1 class="hero__title">NaturHänsyn</h1>
    <p class="hero__subtitle">– för allas vår skull!</p>
    <p class="hero__text">{{ page.header.hero_text|default('Föreningen NaturHänsyn har som syfte att arbeta för att såväl markägare som skogsbolag och myndigheter tar sitt ansvar och visar Hänsyn till Naturen!') }}</p>
    <div>
      <a href="/medlemskap" class="btn btn--primary">Bli medlem</a>
      <a href="/om-foereningen" class="btn btn--outline">Läs mer om oss</a>
    </div>
  </div>
</section>

{# Mission Statement #}
<section class="section">
  <div class="container container--narrow text-center">
    {{ page.content|raw }}
  </div>
</section>

{# Four Pillars #}
<section class="section section--alt">
  <div class="container">
    <h2 class="text-center mb-2xl">Vad gör vi?</h2>
    <div class="pillars">
      {% for pillar in page.header.pillars %}
      <div class="pillar">
        <div class="pillar__icon">{{ pillar.icon|raw }}</div>
        <h3 class="pillar__title">{{ pillar.title }}</h3>
        <p class="pillar__text">{{ pillar.text }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{# CTA Banner #}
<section class="section section--dark text-center">
  <div class="container container--narrow">
    <h2>Vill du göra skillnad?</h2>
    <p>Oavsett om du vill stötta oss som passiv medlem eller aktivt delta i vårt arbete — varje medlem räknas!</p>
    <a href="/medlemskap" class="btn btn--primary">Bli medlem — 100 kr/år</a>
  </div>
</section>
{% endblock %}
